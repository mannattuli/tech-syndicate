<div id="<%= dom_id answer %>">
    <% @user = User.find(answer.user_id) %>
    <% @homework = Homework.find(answer.homework_id) %>
  <p>
    <strong>User:</strong>
    <%= @user.email.split('@')[0] %>

  </p>

  <p>
    <strong>Title:</strong>
    <%= answer.title %>
  </p>

  <p>
    <strong>Desc:</strong>
    <%= answer.desc %>
  </p>

  <p>
    <strong>Submitted at:</strong>
    <%= answer.created_at %>
  </p>

  <p>
    <strong>Due Date:</strong>
    <%= @homework.end %>
  </p>
<br />
<% if answer.picture.attached? %>
    <%= cl_image_tag answer.picture.url, :width=>500, :crop=>"fill" %>
<% end %>
</div>
